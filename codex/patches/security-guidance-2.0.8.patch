--- a/hooks/security_reminder_hook.py
+++ b/hooks/security_reminder_hook.py
@@ -198,6 +198,17 @@
     "response."
 )
 
+def emit_json(output, **kwargs):
+    # Codex does not accept Claude-only telemetry fields in hook output.
+    if os.environ.get("PLUGIN_ROOT"):
+        output = dict(output)
+        output.pop("metrics", None)
+        summary = output.pop("rewakeSummary", None)
+        if summary:
+            output.setdefault("systemMessage", summary)
+    print(json.dumps(output), **kwargs)
+
+
 def emit_metrics(
     metrics,
     rewake_summary=None,
@@ -296,7 +307,7 @@
             }
     if system_message:
         out["systemMessage"] = system_message
-    print(json.dumps(out), flush=True)
+    emit_json(out, flush=True)
 
 # =====================================================================
 # State management
@@ -2334,7 +2345,7 @@
             # Another spawn for this same tool_use_id already claimed the
             # work (compound matched multiple `if` configs). Emit a single
             # metric so telemetry can count how often the de-dupe kicks in.
-            print(json.dumps({"metrics": {"bash_hook_dedup": True}}), flush=True)
+            emit_json({"metrics": {"bash_hook_dedup": True}}, flush=True)
             sys.exit(0)
         if _GIT_COMMIT_RE.search(cmd):
             handle_commit_review_posttooluse(input_data)
@@ -2410,15 +2421,15 @@
                     "hookEventName": "PostToolUse",
                     "additionalContext": PROVENANCE_TAG + "\n\n" + "\n\n".join(all_guidance),
                 }
-            print(json.dumps(output))
+            emit_json(output)
         elif all_guidance:
             # Defensive: pattern rules disabled but guidance somehow set (shouldn't happen)
-            print(json.dumps({
+            emit_json({
                 "hookSpecificOutput": {
                     "hookEventName": "PostToolUse",
                     "additionalContext": PROVENANCE_TAG + "\n\n" + "\n\n".join(all_guidance),
                 }
-            }))
+            })
 
     sys.exit(0)
 
